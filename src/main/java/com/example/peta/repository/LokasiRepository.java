package com.example.peta.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.example.peta.model.Lokasi;

public interface LokasiRepository extends JpaRepository<Lokasi, Long> {
}
